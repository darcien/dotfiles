#!/bin/sh
set -e

if ! command -v brew >/dev/null 2>&1; then
  curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh | /bin/sh
fi

brew bundle install --file="{{ .chezmoi.sourceDir }}/Brewfile"
